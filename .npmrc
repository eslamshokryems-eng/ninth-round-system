# React Native / Expo's Metro bundler does not follow pnpm's strict,
# symlinked node_modules structure well. Hoisting to a flat, npm/yarn-classic-
# style tree is Expo's own documented recommendation for pnpm monorepos:
# https://docs.expo.dev/guides/monorepos/
node-linker=hoisted
shamefully-hoist=true
