# Edge Function: `cloudflare-stream-webhook`

**Phase:** 1 · **Auth:** Cloudflare webhook signature · **Method:** POST

Receives encoding-complete notifications from Cloudflare Stream and flips `exercises.video_status` from `processing` to `ready` (or `failed`).

Not yet implemented — this file documents the contract only.
