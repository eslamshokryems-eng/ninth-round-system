# Web Components

**Phase:** Phase 1

Feature-agnostic UI built on `packages/ui/web` (shadcn/ui-based).

> Scaffolding only — no application code yet. Structure follows `docs/03-folder-structure.md`.
