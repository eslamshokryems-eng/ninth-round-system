# packages/types

**Phase:** Phase 1

Generated Supabase types (`supabase gen types typescript`) plus hand-written domain types that aren't table-shaped.

> Scaffolding only — no application code yet.
