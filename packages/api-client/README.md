# packages/api-client

**Phase:** Phase 1

Typed Supabase client wrapper + TanStack Query hooks. The only package allowed to import the Supabase JS client directly.

> Scaffolding only — no application code yet.
